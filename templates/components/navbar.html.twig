{# templates/partials/_navbar.html.twig #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('booking_form_show') }}">Фотостудия</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if app.session.get('user_id') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('booking_form_show') }}">Форма бронирования</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('booking_list') }}">Мои бронирования</a>
                    </li>

                {% endif %}
            </ul>
            <ul class="navbar-nav">
                {% if app.session.get('user_id') %}
                    <li class="nav-item">
                        <span class="nav-link me-3">
                            {{ app.session.get('username') }}
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('auth_logout') }}">Выйти</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('auth_login_form') }}">Войти</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('auth_register_form') }}">Регистрация</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>