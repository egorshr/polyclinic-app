{% extends 'base.html.twig' %}

{% block title %}Schedule{% endblock %}

{% block body %}
    <h1>Schedule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ schedule.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ schedule.date ? schedule.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TimeFrom</th>
                <td>{{ schedule.timeFrom ? schedule.timeFrom|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TimeTo</th>
                <td>{{ schedule.timeTo ? schedule.timeTo|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_schedule_index') }}">back to list</a>

    <a href="{{ path('app_schedule_edit', {'id': schedule.id}) }}">edit</a>

    {{ include('schedule/_delete_form.html.twig') }}
{% endblock %}
