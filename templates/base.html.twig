<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Фотостудия{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-color: #f8f9fa;
        }
        .page-content {
            flex: 1;
            padding: 2rem 0;
        }
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
        }
        .btn-primary {
            background-color: #5a67d8;
            border-color: #5a67d8;
        }
        .btn-primary:hover {
            background-color: #4c51bf;
            border-color: #4c51bf;
        }
        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease-in-out;
        }
        .card:hover {
            transform: translateY(-5px);
        }
        footer {
            margin-top: auto;
            padding: 1rem 0;
            background-color: #343a40;
            color: white;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('booking_form') }}">
            <i class="bi bi-camera"></i> Фотостудия
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'booking_form' %}active{% endif %}" href="{{ path('booking_form') }}">Заказать съемку</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'show_bookings' %}active{% endif %}" href="{{ path('show_bookings') }}">Мои заказы</a>
                </li>
                {% if app.session.get('role') == 'admin' %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'migrate_data' %}active{% endif %}" href="{{ path('migrate_data') }}">Миграция данных</a>
                    </li>
                {% endif %}
            </ul>
            <div class="navbar-nav">
                {% if app.session.get('user_id') %}
                    <span class="nav-item nav-link text-light">
                            <i class="bi bi-person-circle"></i> {{ app.session.get('username') }}
                        </span>
                    <a class="nav-link" href="{{ path('logout') }}">Выйти</a>
                {% else %}
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'login_form' %}active{% endif %}" href="{{ path('login_form') }}">Войти</a>
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'register_form' %}active{% endif %}" href="{{ path('register_form') }}">Регистрация</a>
                {% endif %}
            </div>
            <form class="d-flex ms-3" action="{{ path('set_storage') }}" method="post">
                <select class="form-select me-2" name="storage_type" onchange="this.form.submit()">
                    <option value="csv" {% if app.request.cookies.get('storage_type', 'csv') == 'csv' %}selected{% endif %}>CSV</option>
                    <option value="db" {% if app.request.cookies.get('storage_type', 'csv') == 'db' %}selected{% endif %}>БД</option>
                </select>
            </form>
        </div>
    </div>
</nav>

<div class="page-content">
    <div class="container">
        {% block content %}{% endblock %}
    </div>
</div>

<footer class="bg-dark text-white mt-5">
    <div class="container py-3">
        <div class="row">
            <div class="col-md-6">
                <h5>Фотостудия</h5>
                <p>Профессиональные фотоуслуги для любых мероприятий.</p>
            </div>
            <div class="col-md-3">
                <h5>Ссылки</h5>
                <ul class="list-unstyled">
                    <li><a href="{{ path('booking_form') }}" class="text-white">Заказать съемку</a></li>
                    <li><a href="{{ path('show_bookings') }}" class="text-white">Мои заказы</a></li>
                </ul>
            </div>
            <div class="col-md-3">
                <h5>Контакты</h5>
                <ul class="list-unstyled">
                    <li><i class="bi bi-envelope"></i> info@photostudio.com</li>
                    <li><i class="bi bi-telephone"></i> +7 (999) 123-45-67</li>
                </ul>
            </div>
        </div>
        <hr>
        <div class="text-center">
            <p class="mb-0">&copy; {{ "now"|date("Y") }} Фотостудия. Все права защищены.</p>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>